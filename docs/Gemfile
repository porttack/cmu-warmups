# Local preview only. GitHub Pages builds this site without needing Gemfile.
#   cd docs && bundle install && bundle exec jekyll serve
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
